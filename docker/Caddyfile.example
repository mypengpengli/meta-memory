{
	servers {
		timeouts {
			read_body 10m
			write 10m
			idle 5m
		}
	}
}

{$MEMORY_DOMAIN} {
	encode zstd gzip

	request_body {
		max_size {$MAX_REQUEST_BODY:70MB}
	}

	reverse_proxy meta-memory:8765

	log {
		output stdout
		format json
	}
}
