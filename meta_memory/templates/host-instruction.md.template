<!-- meta-memory:begin -->
Use the installed Meta Memory Skill for `{{ agent_id }}` on every user turn.
Its launcher path is `{{ launcher_path }}` and its configuration is `{{ config_path }}`.
Use the generated Skill's invocation matching this host's actual shell.
Follow the strict order: `before` → draft the exact final answer → `after` →
send. Send after `ok` or `spooled` only; a semantic `after` error is not a
spooled retry and must be resolved before sending.
<!-- meta-memory:end -->
