"""Legacy implementation modules retained for development compatibility.

Normal users should invoke the ``meta-memory`` command rather than importing
or executing modules in this package directly.
"""
