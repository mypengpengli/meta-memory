interface:
  display_name: "Meta Memory"
  short_description: "Durable local or hosted memory lifecycle for AI Agents"
  default_prompt: "Use $meta-memory to recall bounded context before drafting and save the exact final answer before sending it."
