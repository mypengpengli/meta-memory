<!-- meta-memory:begin -->
For every user turn, use Meta Memory: call `meta-memory before` before answering,
`meta-memory after` after answering, and `meta-memory remember` for explicit
requests to remember a fact.
<!-- meta-memory:end -->
