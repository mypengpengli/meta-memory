<!-- meta-memory:begin -->
Use the installed Meta Memory Skill on every user turn. Follow `before` →
draft exact answer → `after` → send. Only a deferred `spooled` completion may
be sent for later replay; a semantic lifecycle error must be resolved first.
<!-- meta-memory:end -->
